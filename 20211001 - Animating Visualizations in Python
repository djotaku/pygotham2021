Speaker is using Plotly for the animations

It is running within a Jupyter notebook

Better to watch the talk if it's available after the conference because notes would just be step-by-step what the speaker is doing.
